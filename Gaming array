function gamingArray(arr) {  
    
    let count=0;
    let max=Number.MIN_VALUE;
    for(let i=0;i<arr.length;i++){
        if(arr[i]>max){
            max=arr[i];
            count++;
        }
    }
    if(count%2==0){
        return "ANDY";
    }else{
        return "BOB";
    }
